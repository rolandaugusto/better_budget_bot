BOT_TOKEN=your-telegram-bot-token-here

# Optional: path to the SQLite database file. Set this to a path on a
# persistent volume (e.g. /data/expenses.db) in production so data survives
# redeploys. Defaults to a local file next to the project when unset.
DB_PATH=
